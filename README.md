# AI_Quiz_Generator
The project runs in the terminal or command prompt where users answer quiz questions one after another. The program checks each answer, updates the score automatically, and shows the final result after completing all the questions. It also saves the user’s score in a text file for future reference.
